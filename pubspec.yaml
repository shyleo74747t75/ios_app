name: ios_app
description: iOS Remote Access App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  socket_io_client: ^2.0.3
  permission_handler: ^11.0.1
  geolocator: ^10.1.0
  camera: ^0.10.5+5
  record: ^5.0.4
  contacts_service: ^0.6.3
  photo_manager: ^2.8.2
  device_info_plus: ^9.1.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  flutter_local_notifications: ^16.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
